package ru.vkirilchuk.algorithm.grammar.expressions.lexer;

public enum TokenType {
    //TODO: is it just type?
    IDENTIFIER,
    NUMBER,
    PLUS,
    MINUS,
    MUL,
    DIV,
    END_OF_STREAM,
    LEFT_PAR,
    RIGHT_PAR,
}
