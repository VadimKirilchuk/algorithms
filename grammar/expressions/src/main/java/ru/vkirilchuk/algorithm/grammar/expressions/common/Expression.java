package ru.vkirilchuk.algorithm.grammar.expressions.common;

public interface Expression {

    /**
     * Evaluates expression by returning it's computed value.
     *
     * @return computed expression value
     */
    int evaluate();
}
